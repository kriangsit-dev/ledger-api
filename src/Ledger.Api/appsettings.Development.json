{
  "_comment": [
    "Local development only. The signing key and client secrets below are deliberately fake and",
    "the application refuses to start with them outside the Development environment.",
    "For anything real: dotnet user-secrets set \"Auth:SigningKey\" \"<32+ random chars>\""
  ],
  "ConnectionStrings": {
    "Ledger": "Host=localhost;Port=5432;Database=ledger;Username=ledger;Password=ledger"
  },
  "Auth": {
    "SigningKey": "development-only-signing-key-do-not-use-in-production",
    "TokenLifetimeMinutes": 60,
    "Clients": [
      {
        "ClientId": "demo-writer",
        "ClientSecret": "demo-writer-secret",
        "Scopes": ["ledger.read", "ledger.write"]
      },
      {
        "ClientId": "demo-reader",
        "ClientSecret": "demo-reader-secret",
        "Scopes": ["ledger.read"]
      }
    ]
  }
}
